/** Required gap before a Studio Visit when another treatment precedes it. */
export const studioVisitBufferMinutes = 30;

/** Required gap before a Home Service when another treatment precedes it. */
export const homeServiceBufferMinutes = 120;
